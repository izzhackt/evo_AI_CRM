# CLAUDE.md

`AGENTS.md` остаётся правилами работы в репозитории и главнее этого файла.
Здесь — только то, что касается фронтенда V3: направление, границы и порядок
работы над ним.

## Что такое V3

Новый интерфейс, который собирается **рядом** с работающим, а не вместо него.
Никакого «сначала удалим старое»: V2 продолжает работать и остаётся точкой
сравнения, пока V3 не докажет, что лучше.

Живёт в трёх местах и больше нигде:

    src/app/(v3)/         маршруты и образцы данных
    src/components/v3/    части интерфейса
    src/lib/v3/           контракты данных и адаптеры

`/v3` — каталог частей. Каждая часть смотрится **отдельно**: в продукт они
пойдут в разные места, и любая совместная раскладка на этом этапе была бы
случайной композицией, которую потом пришлось бы разбирать.

## Визуальное направление

Взято из референса заказчика — календарное приложение с тёплой подложкой.
Заложено в `src/app/(v3)/v3.css`, областью `.v3-world`.

**Тёплая земля, парящая поверхность.** Подложка розовато-бежевая, не серая:
именно она отличает этот мир от обычного светлого дашборда. На ней плавают
белые панели с крупным скруглением и мягкой тёплой тенью.

**Много воздуха, лёгкая хрома.** Подписи приглушены, рамки волосяные, элементы
управления не спорят с содержимым.

**Скругления от оконной метафоры:** карточка 20px, контрол 12px, навигация
10px. Крупнее, чем в V2, где скругление шло от гранёного знака.

**Красный EVO остаётся акцентом.** Из референса взяты пространство и материал,
а не чужая палитра: `#d70217` — факт бренда из logobook, и он не обсуждается.

### Отличие от V2 — намеренное

В V2 поверхность держит **ребро**, а тень означает только «этот элемент парит
над страницей». В V3 наоборот: поверхность **парит над тёплой землёй**, и это
основной приём. Не переносите приёмы одного мира в другой.

### Правила, которые нельзя нарушать

- **Токены, а не значения.** Компоненты пишутся на `var(--surface)`,
  `var(--accent)`, `var(--text-3)`. Шестнадцатеричный цвет в компоненте
  означает, что он не переживёт смену темы.
- **Смена мира — это смена значений в одном месте.** `.v3-world`
  переопределяет те же имена токенов, поэтому направление меняется без правки
  экранов. Не заводите параллельные имена вида `--v3-surface`.
- **Прозрачность на тексте запрещена.** Цвет, зависящий от подложки, нельзя
  проверить, и он проваливает контраст. Приглушённое — сплошным токеном.
- **Каждое значение проверяется расчётом до того, как попадёт в CSS:**
  текст ≥4.5:1 на каждой поверхности мира, граница контрола ≥3:1 (SC 1.4.11),
  соседние поверхности различимы ≥1.08. Проверять надо и соседние ступени, а
  не только каждую против фона.

## Данные

**Компонент ничего не грузит.** Он рисует то, что ему передали, и знает только
свой тип пропсов. Образцы живут в страницах, а не в компонентах, поэтому
подключение к настоящим данным — это замена одного блока, а не правка вёрстки.

Чего в модели нет — приходит `null` и просто не рисуется. Не выдумывайте
данные, чтобы экран выглядел полнее: дашборд, показывающий несуществующий
рост, хуже пустого.

Платформа сейчас переезжает на Supabase (этим занят другой агент). Поэтому
источники изолированы в `src/lib/v3`: переезд меняет их, а не экраны.

## Порядок работы

Заказчик присылает референс. По нему:

1. один-два вопроса, не больше — только там, где ошибка стоила бы дорого;
   остальное решается самостоятельно и **проговаривается**, чтобы можно было
   поправить;
2. часть собирается настоящим кодом на настоящих данных, где они есть;
3. показывается в прототипе; ссылка одна и обновляется на месте;
4. решение записывается в `docs/design/v3/references/` — что взяли,
   **что осознанно не взяли** и почему;
5. сохраняется локальным коммитом.

**Ничего не сливается в `main` и не деплоится, пока части не будут готовы.**
Сборка в один интерфейс — отдельный этап, и он начинается по явной просьбе.

## Проверки на каждую часть

Тяжёлый набор здесь не гоняется — только то, что дёшево и ловит настоящее:

    npm run build                       собирается ли
    node tmp/audit/v3-parts.mjs         axe, один h1, нет горизонтальной прокрутки

Каждая часть проверяется на десктопе **и** на телефоне 393px.

Повторяющиеся ошибки, которые ловил гейт, — держите их в голове:

- у любой прокручиваемой области обязан быть `tabIndex` и доступное имя, иначе
  мышь прокручивает, а клавиатура нет;
- `aria-label` нельзя вешать на `span`, `p` и прочие элементы без подходящей
  роли; текст для скринридера — через `sr-only`;
- заголовок в карточке не должен сжиматься первым: уступать должны аватары и
  кнопки, иначе событие покажет время без названия.

## Что считается ложью и запрещено

- Писать «сохранено» или «готово» так, что это читается как «выкачено».
  Локальный коммит называется локальным коммитом.
- Показывать выдуманный рост, проценты или суммы там, где сравнивать не с чем.
- Утверждать, что проверено, не запустив проверку.
