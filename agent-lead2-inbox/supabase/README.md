# Supabase

The canonical EVO Platform Supabase configuration, migrations, and SQL tests
live in the repository root at
[`../../supabase/README.md`](../../supabase/README.md).

Do not add SQL files under this companion path. Merged migrations are immutable,
and every future migration starts from the next free number in the root history.
