# База знаний CRM: ход выполнения, 20 сентября 2026

Контракт: [план базы знаний](EVO_CRM_KNOWLEDGE_BASE_PLAN_2026-09-19.md).
Код объединён в main через #902, #906, #915, #916, #918, #919 и #921.
Канонический dirty checkout сохранён; работа выполнена в отдельном worktree
`evo-crm-knowledge-implementation`. Права schema/release, переноса и экспорта
переданы владельцем Astra; Portal/App остаётся за Fable.

## Текущее состояние

Перенос и полная сверка завершены; итоговый ZIP пока не завершён после ошибки Storage.
Следующие разделы содержат исторические снимки: ранние нулевые счётчики,
слово draft и прежние остатки не описывают текущее состояние.

- Admin открывает единый раздел «База знаний»; «Документы» и «Шаблоны
  ответов» находятся внутри него. Прежние рабочие маршруты сотрудников
  сохранены. Новая библиотека, защищённые записи и экспорт доступны Admin.
- Миграции 001–206 применены и сверены с ledger. SOPS runtime, внешний
  age-ключ и maintenance timer установлены; AI bundles не перепубликовывались.
- Сортировка учитывает все 6 570 исходных позиций: 436 внутренних страниц,
  432 внутренних файла, 5 668 файлов сырого архива, 32 защищённых оригинала
  и два ключевых файла вне CRM. Из сейфа добавлены 25 защищённых записей.
- Группа 1 415 закрытых производных перенесена полностью. Первые 920
  исправлены из внутренней области в сырой архив с сохранением версий,
  исходных байтов и происхождения. 32 защищённых оригинала и 25 записей
  перенесены и проверены. Основной обычный поток завершён: 5 121/5 121,
  завершающий проход — 1 182 материала, ноль ошибок.
- У 2 655 исходных материалов сохранены вопросы допуска: 1 415 закрытых
  производных, 912 прочих неутверждённых производных, 328 внутренних
  неутверждённых материалов. Это очередь содержательной проверки, а не
  ошибки копирования. Автоматическое разрешение для клиентского AI не выдано.

## Полнота переноса

Реальная сверка полного плана через интерфейс CRM и тот же прикладной
Admin API дала **6 568 verified, 0 missing, 0 mismatch, 2 ключевых файла вне CRM**.
Исходный объём — 10 445 038 147 байт. 5 961 уникальный blob обслуживает
6 568 исходных расположений; 607 повторов сохраняют отдельное происхождение.
Дополнительно перенесены 25 структурированных защищённых записей.

Проверены разрешённые правки после всех повторных проходов: исходная EVO-страница
осталась на версии 5, рабочая инструкция — на версии 8. Первые 13 ошибок обычной
и 5 ошибок закрытой партии устранены повторным прохождением тех же источников;
их причины не выдаются за установленные. Паузы перед выпусками сохраняли
принятые части: 7 и 11 прерываний оператором учитываются отдельно от ошибок
источников. Оригинальная локальная база не удалялась и не переименовывалась.

Приватная квитанция `Полная сверка источников.json` содержит строку каждого
источника, SHA-256, размер, node/blob, область и результат. Исходный план и
инвентарь, контрольные ZIP и защищённые квитанции находятся вне Git, в
`EVO_Перенос_базы_знаний_CRM` (каталог0700, файлы0600).

## Дополнительная реальная приёмка после #919

На production `49f071488972445b2c4fded63a1e6cba7d8ab0bb`:

- Пустое несохранённое название рабочей инструкции вызывает конкретную
  ошибку. Переход через меню и браузерный Back остаётся в редакторе;
  явный отказ от правки возвращает сохранённое состояние. Версия инструкции
  осталась 8; содержимое не потеряно и не перезаписано.
- Вопрос у существующего QA-файла добавлен через интерфейс, сохранён,
  повторно открыт и очищен. История содержит вопрос в версии 3 и его
  разрешение в версии 4; blob и происхождение версий 2–4 совпадают.
- Через досье выбран один существующий документ и подготовлен ZIP
  `df7419f0-c62d-4c04-afcc-0e4e53ffb1f1`. Событие скачивания в браузере
  подтверждено; отдельная копия того же задания скачана штатным Admin HTTP
  для побайтовой сверки: один файл, 7 357 633 байта, без переписки и других
  материалов; SHA ZIP `580d770e7f20d30015100f90c30951bfcd4bcfdf562e270ab76cfa998106df35`.
- Прямой переход в корень документов компании показывает существующую
  карточку `testing1.txt`; у неё нет версии файла. Наполненных канонических
  company files и snippets в этой БД нет, поэтому их содержательный поиск
  и скачивание не заявлены как пройденные на реальных данных.
- Папка «ИИ-ассистент / Разборы и исправления» создана и проверена через UI.
  Искусственные клиентские записи не создавались.

HTTP/UI проверка canonical search обнаружила 403 `platform_route_not_connected`
до входа в Admin handler, хотя прямые Auth/RPC проверки 205 проходили.
#921 добавляет только точный endpoint в существующую proxy-границу;
25/25 route checks и независимое ревью точного `b7cb9396` пройдены.
После выпуска `a0684b40` реальный HTTP-путь прошёл: Admin200, Staff403,
QA Student401, anonymous401. Браузер нашёл существующий документ, переход
открыл его досье и очистил строку поиска.

Выпуск `35478761736` принят на `49f07148`, pending удалён, arm выключен;
SHA acceptance record `a7163f989f849b2716cb7c64730b760a3ce03e88859a0874279b9e8dc53c78d5`.
Следующий выпуск `35479233455` собрал `a74cec44`, но безопасно остановился
на deploy admission: в активном freeze вошёл docs-only #920, main стал
`a0684b40`. Сервер остался на `49f07148`; обход exact-main не выполнялся.
Повторный выпуск `35479396937` принят на exact main
`a0684b404c152c51ecdf47037c72c6f4722185b7`; OCI revision и accepted pointer
совпадают, container healthy, restart0, pending отсутствует, arm=false
подтверждён readback. SHA acceptance record
`54ef54609ee4c24480e4fd4af2563952e6088ba968e812748ba1380cfc742751`.
Координация и снятие freeze отражены в комментариях #920, #921 и #913.

## Исторические снимки выполнения

## Реализованный кандидат

Этот раздел и следующий инвентарный снимок относятся к подготовке #906 до
его выпуска; дальнейшие реальные результаты записаны в дополнениях ниже.

- Admin-библиотека: папки, страницы Markdown, файлы, поиск, входящие, вопросы,
  архив, корзина, версии, конфликтующие правки, стабильные ссылки.
- Импорт папок и исходной базы: проверка SHA-256 и размера, части по 8 МиБ,
  возобновление, дедупликация внутри области, сохранение происхождения,
  повторный импорт без перезаписи правок в CRM, полная сверка источников.
- Досье из существующих дел, документов, чата и истории. Исторические
  материалы привязываются только к явно выбранному делу после подтверждения;
  привязка папки охватывает её потомков. Первичные объекты CRM не меняются.
- Отдельные рабочие страницы документов и шаблонов ответа сохраняют доступ
  сотрудников. Новая библиотека и её API остаются Admin-only.
- SOPS-записи и зашифрованный перенос локального сейфа. Значения не входят
  в поиск, обычные таблицы, квитанции или массовый plaintext-экспорт.
- ZIP64 по материалам, папкам, досье и всей базе: фиксированный снимок,
  версии/архив/корзина по выбору, относительные ссылки, оригиналы, манифест,
  контрольные суммы, повторное использование проверенных частей. Ошибка
  содержит проблемный материал; неполный архив не выдаётся за готовый.
- Фоновая подготовка и повторная очистка временных экспортов. Предложены
  systemd timer и SOPS в immutable image. Срок доступа к выгрузке — 24 часа.

## Источники и фактический перенос

Приватные инвентари и планы находятся вне Git, в локальном каталоге
`EVO_Перенос_базы_знаний_CRM`. Имена клиентов и содержимое источников здесь
не публикуются. Исходная база сохранена без удаления и переименования.

| Показатель | Факт |
|---|---:|
| Файлов в исходных четырёх контурах | 6 570 |
| Исходников с SHA-256 | 6 568 |
| Ключевых файлов вне переноса | 2 |
| Будущих редактируемых страниц | 436 |
| Обычных исходных файлов | 6 100 |
| Защищённых оригиналов | 32 |
| Структурированных записей из существующего сейфа | 25 |
| Подготовленных и проверенных SOPS-объектов | 57 |
| Записанных plaintext-файлов при защищённой подготовке | 0 |
| Фактически импортировано в новую CRM-библиотеку | **0** |
| ZIP из новой CRM скачан и сверен | **Нет** |

При классификации разобраны заголовки и признаки допуска только разрешённых
рабочих/одобренных Markdown. Сырые контейнеры, Trash/Spam и чувствительные
документы не извлекались, не OCR-ились и не отправлялись внешним AI.
Прежняя клиентская AI-база распределена как общие знания с прежним
происхождением; опубликованные AI bundles не изменены.

## Выполненные проверки и их пределы

- Реальный инвентарь возобновлён после прерывания; все 6 570 позиций учтены.
- Защищённая подготовка повторно проверила 57 SOPS round trips;
  ciphertext — 27 167 094 байта. Ключ получен процессно из Keychain.
- Два настоящих зашифрованных объекта, включая самый большой оригинал,
  прошли серверный SOPS-код в Linux/Node 22. Ключ жил только в tmpfs
  временного контейнера. Это проверка механизма, не production-провижининг.
- Четыре SQL-модуля применены к отдельной локальной БД поверх копии схемы
  и реальных миграций 126–200. `plpgsql_check` не нашёл ошибок/предупреждений
  в KB-функциях и триггере. Auth и клиентские строки не копировались.
- Сборка Next.js и трёх worker bundles прошла на Node 22.23.1.
- Точечные ESLint, проверки маршрутов/навигации и политики доступа пройдены.
  Это проверки кода; они не заменяют реальные отрицательные Auth/RLS-пути.
- В браузере подтверждён реальный вход Admin в действующую старую CRM.
  Новый интерфейс, Storage-перенос и экспорт через новую схему ещё не проверены.

Независимое ревью нашло и помогло устранить ошибки прав/навигации,
повторных запросов, смены пользователя, пересекающегося выделения,
экспорта незавершённых загрузок, атомарной записи ciphertext и TTL-очистки.
Окончательный вывод относится только к указанному в PR неизменному commit.

## Что необходимо до завершения

1. Владелец передал Astra schema apply / выпуск, перенос и экспорт. Fable
   уведомлён в #912. За KB зарезервированы forward migrations 201–204;
   production ledger до применения — 200, arm выключен.
2. Выполнить ревью точного head с миграциями и protected checks. Применить
   через общий ledger и проверить
   реальные RPC/RLS. Порядок: base → secrets → canonical export → export.
3. Согласованно подключить внешний ключ `/etc/evo-crm/knowledge/age.key`,
   публичного получателя и maintenance timer. Ключ не входит в checkout,
   build context или образ. Не печатать его в квитанциях и журналах.
4. Провести управляемый выпуск с exact-main, одним immutable build,
   коротким smoke новой функции и сохранённым rollback.
5. В реальной CRM проверить Admin и прямой отказ Staff/Student, редактор,
   версии/конфликт, файлы, секретный reveal/copy без печати значения,
   существующие рабочие документы, досье и узкий/mobile layout.
6. Выполнить контрольную партию настоящих источников: импорт → чтение и
   редактирование → скачивание оригинала → ZIP → SHA-256. Затем перенести
   все 6 568 разрешённых исходников и 25 производных защищённых записей.
7. Сверить каждый source key и оригинал, выгрузить полную разрешённую базу
   и прогнать `verify_export.py --verify-protected --require-all`. Записать
   итоговые количества, исключения, ошибки, точный релиз и путь квитанции.

Применение схемы, provisioning ключа, установка timer, production arm и
массовый импорт в этой квитанции не заявляются выполненными.


## Production: применение схемы и запуск выпуска

Владелец передал Astra schema apply, выпуск, перенос и экспорт. Fable уведомлён
в #912; резерв 201–204 и exact-main freeze опубликованы до применения/dispatch.
PR #906 принят в main `2e72d5ad01c921979646b781fbfb04e591f63e7b` после
независимого ревью `d26e1c1f79fd364bd3c64a6609d30792ebcf32b0` и успешного
protected run `35472888607`, включая Migration boundary.

- Миграции 201–204 применены одной транзакцией с общим ledger. Текст получен
  через `git show` из одобренного commit; SHA-256 всех четырёх записей ledger
  совпали с исходными SQL. Номера/хеши приведены ниже.
- Создан private bucket `platform-knowledge-library`, лимит части 8 MiB.
- Через настоящий password Auth существующий Admin получил KB RPC 200;
  Sales Manager — 403/42501, существующий постоянный QA Student — 403/42501,
  anon — 401/42501. Новые пользователи/роли/дела не создавались. QA Student
  подтверждает техническую границу, не приёмку настоящим клиентом.
- Существующий age key установлен process-only из Keychain во внешний
  `/etc/evo-crm/knowledge/age.key`: uid1001,0400; каталог0700. В environment
  добавлен только публичный recipient. Приложение ещё должно подтвердить
  фактическое использование ключа после выпуска.
- Exact-main CI `35473267225` запущен с `proof_revision` этого main;
  downstream managed release `35473287436` принят как
  `v3-r35473287436-a1-2e72d5ad`. Accepted pointer совпал, pending отсутствует,
  arm=false. Maintenance timer установлен, первый запуск Result=success.
  22 HTTP/RPC-проверки новой границы пройдены. Student API отвергается штатным
  staff-gateway с401; прямой KB RPC —403. Это отличается от отсутствующей сессии:
  собственная Student authority подтверждена обычным Auth.

| Миграция | SHA-256 SQL в ledger |
| --- | --- |
| 201 | `211c75f3f92b7254702d517b62bca31f26d6fd0f7e07e37b5edcc620fcefb69d` |
| 202 | `924f0d70195e7110e42d058fb28459d09670ecf1c12b7ed363f74033c0b1ae5b` |
| 203 | `c90155f88a547c0d62f9db74950416053b351c84a089c4cb5aabfd5308826e3c` |
| 204 | `361f197dcbbbb742938506e9cc78792c719024bcf8c673940e3fc011ab11fcd4` |


## Контрольная партия и найденные исправления

Через production UI перенесены 10 реальных источников: 2 страницы, 8 файлов,
3 049 345 байт; 9 source blobs с сохранением обеих позиций одинакового файла.
ZIP получен через кнопку CRM: 19 материалов проверены по манифесту, байты всех
10 перенесённых оригиналов совпали с исходным инвентарём. Это не полный экспорт
всех локальных источников; protected originals пока не импортированы.

Приватные артефакты: `Контрольный экспорт CRM.zip`,
`Проверка контрольного экспорта.json` в каталоге переноса. Git содержит только
агрегаты. История и автосохранение реальной страницы проверяются отдельными
операциями; завершёнными до проверки они не объявляются.

Перед полным переносом исправляются обнаруженные старые Notion-ссылки,
читаемые заголовки и распределение старой очереди. Новый план сохраняет все
source keys/node ids и исходные байты: 436 страниц + 1 847 файлов внутри,
4 253 файлов в сыром архиве, 32 защищённых оригинала, 2 ключа снаружи.
912 прежних извлечённых текстов отнесены к производным исходников в архиве;
остальные прежние кандидаты сгруппированы среди процессов подготовки базы.
Ни один кандидат не становится автоматически утверждённым. Новых материалов
в папку «Входящие» план не направляет. Сырой корневой служебный файл сохраняет
корень своего архива. Оригинальный план до проверки сохранён приватно.

Последовательный контрольный импорт выявил сетевую задержку около 8 секунд
на небольшой файл. Следующий кандидат переносит до четырёх независимых файлов
одновременно, сохраняя последовательность для одинакового blob, общую остановку
и честный список ошибок. Реальная проверка параллельной партии после его
выпуска ещё необходима; tsc, scoped ESLint и генерация плана на всех настоящих
источниках уже прошли. БД/роли/права не меняются.

## Follow-up: объединённая навигация и реальный защищённый перенос

- #915, reviewed head `44acb1cbfe74e75cca9aac9a5921dc7a2b2dd558`, short CI
  `35474377147`; main `1635d01eff7b70f81f2e19b6e03ffba4f5316dc4`.
  Managed release `35474499486` принят, app healthy, pending отсутствует,
  затем arm=false. Acceptance SHA-256:
  `a96b4df1bebc7e563367f70ac5470ff9fce6f827d301f7bd90462c3797a7871a`.
- Реальный Admin-браузер подтвердил один основной пункт «База знаний», старый
  `/v3/documents` перенаправляется в `?section=documents`; шаблоны открываются
  в `?section=snippets`. Через обычный Auth Admin и Admissions Manager прошли
  пять HTTP-проверок серверных экранов/навигации и отказа staff в KB API.
  Предварительный запуск этой проверки ещё на старом образе fd25b1ac ожидаемо
  обнаружил старое меню; успешный запуск привязан именно к новому образу.
- В реальном UI перенесены 32 защищённых оригинала и 25 записей доступа.
  Все 25 metadata/value повторно прочитаны через штатный Admin API и сравнены
  в памяти с исходными зашифрованными записями: совпадают. Metadata не содержит
  value. Проверены UI reveal/hide/copy и save с сохранением текущего значения;
  после copy буфер заменён обычной ссылкой. Plaintext-файлы не создавались.
- Sales Manager и существующий QA Student получили ожидаемые отказы на прямой
  reveal реальной записи и download реального export job: ещё четыре проверки.
- Редактор: сохранён настоящий H1, исправлены две подтверждённые ссылки,
  восстановлена версия 2, затем актуальная версия 3 как новая версия 5.
  Старый expectedVersion=3 возвращает 409 и не меняет текущий текст/версию.
  Скачанный original MD совпал с инвентарём SHA-256.
- Новая контрольная партия реально прервана перезагрузкой вкладки во время
  загрузки, затем возобновлена через тот же UI. 10/10, ошибок 0; повторно
  найденные источники не создавали новые материалы и не перезаписывали правки.
  Серверная сверка подтверждает 50 источников: 18 обычных + 32 защищённых,
  уникальные id, area, размер, SHA-256, ready blob; редакция страницы остаётся v5.
- Через UI скачан отдельный ZIP страницы с двумя связанными CSV:
  job `1362a86c-aa76-4018-91c9-e1a88e52292e`. Verifier проверил три оригинала;
  обе ссылки стали относительными и разрешаются в реальные файлы ZIP.
- Полный обычный импорт начат четырьмя потоками. Перед выпуском #916 штатно
  остановлен после 71 записи, ошибок 0. Общая фактическая база в этот момент:
  46 страниц + 28 обычных файлов + 32 защищённых оригинала + 25 записей доступа.
  Это промежуточный снимок, **не завершение полного переноса**.
- #916, reviewed head `7e86c8ddac04adcb80f72a1f82849c2a55d4d0a2`, short CI
  `35474875855`, main `9004640539a02b8a8c7b913f7f170c0b44464b50`: исправлены
  вложенное дерево документов и повторный заголовок шаблонов, найденные живым
  просмотром. Выпуск и окончательная визуальная проверка на момент записи идут.

Приватные ZIP и подробные квитанции остаются в отдельной локальной папке
переноса; значения секретов, переписка и личные документы в Git не внесены.

## Дополнение: единая навигация и клиентское досье

- #916 выпущен через managed run `35475040133`: принят
  `v3-r35475040133-a1-90046405`, exact main
  `9004640539a02b8a8c7b913f7f170c0b44464b50`. Acceptance SHA-256:
  `4189c25c46c14bcc619cbbac4bb635d382388015660618c7f854a140e1e059ef`.
  Проверены healthy image, отсутствие pending и readback arm=false.
  Freeze снят в #916/#913; остановка приложения во время импорта требует
  координации для штатной паузы, а не длительной блокировки GitHub main.
- На этом образе повторены пять реальных Auth-проверок Admin/staff.
  В браузере: документы используют одно дерево библиотеки; у шаблонов один
  заголовок, проверен узкий экран 760 px, временный viewport сброшен.
- Реальный ZIP существующего QA-досье:
  `aa2204b0-0244-4b08-8af6-40cd952e2da0`, семь материалов. Вкладка перезагружена
  при «Подготовка · 0 из 7»; то же задание завершилось и было скачано.
  Все суммы совпали. Файл 7 357 633 байта из ZIP также совпал с байтами
  действующего `/api/v2/document-versions/.../download`, с тем же version ID.
  Дело, документы, переписка и история прочитаны через обычный Admin Auth.
  Это техническое доказательство на существующей QA-личности, не приёмка
  реального клиента.
- Через обычную загрузку папки добавлены два файла фактической квитанции этой
  проверки. После выбора точного существующего дела и явного подтверждения
  принадлежности папка и оба потомка получили тот же case ID. Первичный
  документ не копировался; клиентских дел осталось два. Байты вложений
  сверены с локальной квитанцией. ZIP выбранных двух файлов
  `c4840b9c-214c-4f2c-ba2a-3c09a3c737b8` содержит ровно эти файлы.
- Создана рабочая инструкция по новой базе. Проверены создание папки/страницы,
  автосохранение, перемещение туда и обратно с сохранением постоянной ссылки,
  корзина и восстановление. ZIP папки `d8851317-3bbe-491c-a6cf-9a6643cbd3ae`
  содержит папку и восстановленную страницу v6; SHA-256 совпал.
- Обычный браузерный импорт штатно остановлен после 123 записей без ошибок
  (всего 126 обычных источников с учётом прежних партий). Массовое продолжение
  выполняется тем же `importKnowledgeFile` через штатный Admin HTTP API,
  с процессным обычным Auth, проверкой SHA/размера, source keys и одинаковым
  механизмом идемпотентности. Отдельный read-only просмотр runner не выявил
  дефектов. После измерения нагрузки параллелизм повышен с 12 до 24;
  одинаковые blobs обрабатываются по очереди, после трёх ошибок — остановка.
  Доступ через service role или прямые записи в таблицы для переноса не
  используется. Полный перенос и полный ZIP на момент этой записи ещё идут.

## Дополнение: исправленная сортировка закрытых производных

По метаданным выявлено ошибочное наследование старой внутренней области для
1 415 закрытых производных WhatsApp. Содержимое не анализировали, AI bundles
не меняли. Все такие материалы теперь запланированы как файлы в сыром архиве,
в «Производных исходников / Закрытых производных материалах», с вопросом
о назначении и подтверждённой принадлежности.

- На полном приватном инвентаре проверены все 6 570 записей: ID, source key,
  SHA-256, размер, вид и действие сохранены. Только у этих 1 415 изменились
  область, папки и вопрос. Для остальных записей план совпал полностью.
- Из группы 920 файлов были уже загружены. Их новые raw blobs подготовлены
  и проверены через обычный Admin API. Независимое review одобрило exact head
  `0fd0d5989dfbd174c1da2d3ebf3405b2d254e790`; short CI `35476667925` прошёл.
- Ограниченный SQL-шаблон `scripts/knowledge-library/refile_restricted.sql`
  сначала выполнился с ROLLBACK: все 920 строк прошли проверки. Затем тот же
  вход применён с COMMIT. Схема, Auth и grants не менялись. Это явно выделенная
  коррекция данных, в отличие от обычного импорта через API.
- Template SHA-256:
  `680ac0b32890e4b5218ec4c7cf17c59b32dd2a006fad09e495d9f6d8c0f6a1a0`;
  input SHA-256:
  `4abbf22a8655a937a6ced9f3303e67b33c726b0d93e77452343bd01c9fabc1b0`;
  commit command SHA-256:
  `75c0e19c6aeeaa0ef214ce3436b6b4ee882c0523557f66c23877387e12ab65c2`.
- После commit обычный Admin API подтвердил все 920 source IDs, raw-область,
  версию 2 и готовые исходники с теми же SHA/размерами. Отдельное чтение БД
  подтвердило 920 прежних версий и старых blobs. Реальное скачивание одного
  оригинала совпало по байтам. Повторная транзакция с откатом: moved=0,
  alreadyApplied=920; новых версий не появилось.
- Первая попытка дополнительного скачивания через Python urllib упёрлась
  в отсутствующий локальный issuer CA. Повтор выполнен системным curl с
  обычной TLS-проверкой; проверка сертификата не отключалась.
- Десять оставшихся пустых папок старого размещения проверены на отсутствие
  материалов, затем перемещены в восстанавливаемую корзину через Admin API.
  Исходные файлы и исторические blobs не удалялись. Остальные 495 файлов
  группы загружаются по исправленному плану.

После исправления план содержит 436 внутренних страниц, 432 внутренних файла,
5 668 файлов сырого архива, 32 защищённых оригинала; два ключевых файла остаются
вне переноса. 2 655 материалов имеют вопрос для уточнения. Это сохранение
неутверждённых/закрытых источников, а не автоматическое подтверждение знаний.

## Дополнение: конфликт правок и поиск

Рабочая инструкция прошла настоящий конфликт между двумя вкладками:
вторая вкладка со старой версией показала ошибку и сохранила несохранённый текст.
После явного «Сохранить мой текст» получена версия 8; её заголовок, текст,
папка и отсутствие отметки удаления прочитаны повторно через Admin API.
Поиск по фрагменту тела страницы из сырого архива нашёл внутреннюю инструкцию
в режиме «Вся база». Режим «Текущая папка» исключил её, показав пустой результат.
