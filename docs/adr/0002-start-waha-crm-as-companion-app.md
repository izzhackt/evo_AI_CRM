# Start WAHA CRM as a companion app

> Status: superseded by ADR 0020; companion-era historical source only.

The WACRM-derived WAHA CRM will start as a standalone companion app inside this repository instead of replacing the current EVO Operator UI. This keeps the existing production CRM and lead-agent path intact while the new WhatsApp-first surface is rebranded, converted from Meta Cloud API to WAHA, and proven on the VPS.
