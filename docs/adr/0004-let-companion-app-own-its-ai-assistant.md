# Let the companion app own its AI assistant

> Status: superseded by ADR 0020; companion-era historical source only.

The Companion WAHA CRM App will run its own AI reply assistant instead of delegating draft review or auto-replies to the existing EVO lead-agent. This keeps the WACRM-derived product line parallel for evaluation while preserving the rule that amoCRM remains the identity source of truth.
