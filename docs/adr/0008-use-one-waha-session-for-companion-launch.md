# Use one WAHA session for companion launch

The Companion WAHA CRM App will start with one WAHA session named `evo-inbox`. A single session keeps QR setup, webhook routing, amoCRM identity matching, and first production proof narrow before any later multi-number or multi-account model is introduced.
