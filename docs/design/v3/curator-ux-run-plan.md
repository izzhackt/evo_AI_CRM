# EVO V3: кураторы и лёгкий UX/UI — отдельный план

Обновлено: 2026-09-08. Новый запуск, отдельный от завершённого первого релиза.

## 08.09 — одобренные улучшения кураторов и лёгкий UX/UI

**Статус: код пяти одобренных пунктов подготовлен; владелец разрешил выкладку
без недоступной сейчас приёмки на реальном деле. Технический release и новый
Sales-отчёт ещё не готовы.**
Основание — [решение владельца](product.md#owner-curator-ux-20260908).
Общий контракт — [EVO_LAUNCH_PLAN.md](../../EVO_LAUNCH_PLAN.md), изменения
границ — [PLAN_CHANGES.md](../../PLAN_CHANGES.md). Сначала читать AGENTS.md,
CONTEXT.md, CLAUDE.md и действующие frontend rules; вести статус этой волны здесь.
Номера ниже относятся к списку из 14 рекомендаций от 08.09, а не к трём
блокам первого запуска. Детали исполнения ниже — минимальная декомпозиция
этого решения, не дополнительные слова владельца.

Первый запуск уже закрыт: [#552 acceptance](https://github.com/izzhackt/evo_AI_CRM/issues/552#issuecomment-5576220221)
и [#553](https://github.com/izzhackt/evo_AI_CRM/issues/553) подтверждают принятый
`4e6057f0159dba6515ea18b412567b48779cf77f`, full CI `34165496188` и release
`34165979423`. Это прочитанная 08.09 GitHub evidence, не новая проверка VPS.
Новый docs commit не меняет deployed revision и не требует повторить закрытый
запуск. Исторический pre-freeze checkpoint сохранён в [старом плане](run-plan.md)
для расследования; его незакрытые старые checkbox не являются очередью этой волны.

### Решение владельца о выкладке без реального дела

В production сейчас нет доступного Admin дела, на котором до релиза можно
выполнить настоящие действия Curator, Sales и Student-self. Владелец явно
разрешил выложить эту волну без такого pre-deploy business-case acceptance и
проверить интерфейс после выкладки. Это **waiver/defer**, а не пройденная
приёмка: checkbox реальных ролей ниже остаются незакрытыми, и создавать для них
synthetic users/cases/data нельзя.

Разрешение не отменяет технические gates: reviewed exact SHA и current `main`,
обязательные CI checks, backup, rollback/recovery, проверку migration history,
точного образа и production health/readback. По более позднему явному решению
владельца migration 131 добавляет `student_portal_overview_v2()` и не меняет
существующий `student_portal_overview_v1()`. Старый v1 остаётся только на
временное rollback-окно до проверки владельцем; новый UI вызывает строго v2 и
не делает fallback на v1. После принятия портала v1 удаляется отдельной reviewed
forward migration. Provider calls в эту авторизацию не входят и не разрешены.
8 сентября владелец подтвердил предложенный путь выкладки: временно разрешить
отсутствующий `p_reason` только прежнему Admin-вызову канонического body migration
129, которое переиспользует 133. Переданная пустая причина остаётся ошибкой,
Curator reason-required и все ownership/coverage guards сохраняются. Новый UI
всегда передаёт причину; overload не добавлять. После owner acceptance удалить
исключение отдельной forward migration вместе с временным portal rollback окном.
Существующие изолированные CI fixtures разрешены только для технических gates,
а не как реальная business/provider acceptance.

### Новый блок: Sales-отчёт вместо Google Sheet — отдельный контракт

Владелец также поручил создать и выложить отчёт Sales вместо Google Sheet:
один перенос реальных данных, затем работа в платформе, без sync. Источник из
209 строк и 4 месячных планов отдела проверен; контракт до coding записан в
[sales-report-run-plan.md](sales-report-run-plan.md). Там же — checksum,
приватные артефакты, schema/UI, автоматическая запись после handoff, проверки
и оставшиеся blockers. Общая интеграционная ветка — `izzhackt/sales-report-platform`;
production import/deployment не закрыты.

### Точная граница выбора

| Исходный пункт | Решение | Что входит |
| --- | --- | --- |
| 2 | Approved | Внутренние сроки и приоритет собственных задач куратора |
| 3 | Approved | Явное принятие дела после передачи Sales, запрос уточнения |
| 4 | Approved | Единая разрешённая история студента |
| 6 | Approved | Нагрузка, отсутствие и замещение кураторов |
| 8 | Approved | Понятный следующий шаг в существующем кабинете студента |
| 9 | Rejected | Не добавлять отдельный контур родителей/плательщиков |
| 10 | Deferred | Не делать сейчас сравнение offers и развитие каталога из рекомендации |
| 13 | Deferred | Не добавлять сейчас новый post-arrival/адаптационный контур |
| 1, 5, 7, 11, 12, 14 | Not approved | Не включать в эту волну; отсутствие ответа не approval |

Отложенные расширения не означают удалить уже существующие заявки, контакты,
визовые этапы или данные. Нет новой роли, второй CRM, смены Auth, design system,
роутера или отдельного экрана «Мой день». Сохраняются три staff-роли, текущий
светлый V3, общий document starter и состояния документов «нет / есть».

### Небольшие исполнимые части

- [ ] **UX-2 — рабочие права куратора.** Изменять срок и приоритет только своей
  назначенной операционной задачи в доступном ему деле; видимость студенту,
  исполнитель, финансовые полномочия и университетские дедлайны не меняются.
  «Внутренний срок» здесь означает срок операционной задачи, а не обязательно
  `student_visible=false`. Править не только форму: запрет находится и в RPC
  из migration 110. Использовать reviewed forward migration, существующие
  `platform-admissions-task-actions.ts` и `calendar/TaskControls.tsx`.
  Приёмка: реальное разрешённое изменение, durable readback и audit с причиной;
  чужая задача, изменение исполнителя/видимости и stale version отклоняются.
  Сохранить различие date-only/time и организационную зону Asia/Bishkek.
- [ ] **UX-3 — принятие передачи.** Текущий назначенный куратор подтверждает
  ознакомление либо просит конкретное уточнение; состояние и следующий шаг
  видны в существующей карточке. Это отдельное подтверждение поверх уже
  атомарного handoff, не откат договора/дела и не повтор starter tasks.
  Не закрывать доступ до принятия и не ставить выдуманный срок первого контакта:
  использовать согласованную дату. Начать с `platform-student-handoff.ts`,
  migration 088 и `profile/ProfileSalesTransition.tsx`.
  Приёмка: реальное принятие текущим владельцем и запрос уточнения сохраняются;
  повтор команды не дублирует события; посторонний не подтверждает. Admin не
  выдаёт своё действие за принятие куратором. При смене владельца сохранять
  прежнюю историю, но не приписывать его подтверждение новому куратору.
- [ ] **UX-4 — история в существующем профиле.** Расширить `caseTimeline()` в
  `src/lib/v3/profile-source.ts`, начав с `staff_student_case_activity`
  (migration 089). Соединить разрешённые события передачи, задач, заявок,
  документов, платежей и переписки; краткая строка ведёт к исходной записи.
  Не отдавать raw audit payload, не копировать переписку во второе хранилище.
  Словарь, стабильный порядок, дедупликация и пагинация едины. События изменения
  только срока/приоритета взять из реального audit: task transition stream
  их сейчас не содержит. Приёмка: видны существующие реальные события и новые
  события UX-2/3/6, без дублей; роли получают только разрешённые сведения.
  Не открывать полную Admissions-историю Sales/студенту; прежний запрет
  показывать автора/время приложения документа отдельно не отменён.
- [ ] **UX-6 — нагрузка и замещение.** Добавить компактное представление Admin
  в существующую поверхность: активные дела, открытые задачи и ближайшие сроки
  по кураторам; без выдуманного процента занятости, рейтинга и HR-модуля.
  Минимальный вариант замещения — один канонический владелец, временное
  переназначение Admin с причиной/сроком и явным возвратом; не параллельный
  доступ двух владельцев и не новый автоматический scheduler.
  Переиспользовать `assign_student_case_curator` и invoker-wrapper из 117.
  До изменения показать Admin затронутые дела и открытые задачи: нынешняя
  смена владельца сама задачи не переносит. Закрытые задачи/историю не менять.
  Приёмка: агрегаты сверяются с реальными доступными данными; при действительно
  необходимом замещении меняются ownership, scope/access version и выбранная
  открытая работа, возврат подтверждён. Нет частично успешного сообщения,
  осиротевших задач, доступа у неактивного заместителя или скрытого расширения прав.
- [ ] **UX-8 — ясный кабинет студента.** В `portal/OverviewView.tsx` оставить
  компактные ответы: что требуется от студента, что делает EVO, какой следующий
  шаг и срок; далее прямой переход к существующему документу/задаче.
  Новая форма `student_portal_overview_v2` и `portal-source.ts` становятся
  источниками истины для нового UI. Неизменённый v1 не используется как fallback
  и существует только для rollback прежнего приложения до проверки владельцем.
  Не угадывать исполнителя по тексту: если его нет в проекции, сначала добавить
  разрешённое каноническое поле. Ожидание извне показывать лишь когда оно
  подтверждено данными, без нового механизма ожиданий из неутверждённого №5.
  Приёмка: реальный student login, совпадение шага/даты с источником, рабочая
  ссылка и честное пустое состояние; внутренние задачи/заметки и чужие файлы
  не раскрываются. Нет parent portal, нового reminder engine или рассылок.
  Live invite/SMTP proof остаётся отдельным gate, не обещанием от UI-правки.

### UX/UI: критерии для этой волны

Лёгкая коррекция существующих экранов, **не большой редизайн**. Применить ко всем
изменяемым формам, карточке/истории, представлению замещения и portal overview;
общие компоненты поправить один раз. Не запускать отдельный тотальный аудит
или миграцию UI-библиотеки. Сохранить [frontend rules](frontend-rules.md).

1. **Понятный статус.** Видимые загрузка/сохранение/успех/ошибка/пустота;
   «Сохранено» только после подтверждённой записи. Не показывать бесконечный
   spinner вместо ошибки или старые данные как свежие. При ошибке поля сохраняются.
2. **Знакомый язык.** «Принять дело», «Перенести срок», «Нужно уточнить», а не
   RPC/enum/UUID. Использовать существующий `wording.ts`; технические детали
   не превращать в постоянные поясняющие баннеры.
3. **Контроль и свобода.** Явные «Отмена»/«Назад», предсказуемое закрытие формы,
   сохранение контекста списка. Undo только для действительно обратимых действий;
   для необратимых — понятное предупреждение/подтверждение, не фиктивная отмена.
4. **Последовательность.** Те же токены, формы, названия, кнопки и состояния
   на всех затронутых экранах. Существующий светлый V3, не новый визуальный мир.
5. **Защита от ошибок.** Проверка полей рядом с вводом, понятная причина запрета,
   защита от двойного submit и конфликта версий. Перед сменой куратора — точный
   состав переноса; подтверждение не требуется для каждого безобидного клика.
6. **Фиттс: удобно попасть.** Частые действия рядом с объектом, главная кнопка
   заметна и имеет подпись. Ориентир для частых touch-controls — 44×44 CSS px;
   не сжимать всё ради плотности. Keyboard focus и доступное имя обязательны.
7. **Хик: не перегружать выбором.** Одно главное действие на активную форму,
   вторичные — спокойнее; редкие настройки раскрываются по запросу. Не прятать
   ежедневное действие в цепочку меню и не убирать нужные возможности ради числа.
8. **До трёх понятных переходов для частых задач.** Цель: от контекстного
   списка открыть нужную форму/информацию без блуждания. В приёмке записать
   старт, переходы и полный путь включая ввод/подтверждение; не выдавать
   сокращённый navigation count за весь процесс. Три клика — целевой ориентир,
   не универсальный закон и не основание убрать важное подтверждение.

**Общая граница — не dense.** Это отдельное требование владельца, не девятый
UX-принцип. В обзоре только статус и следующий шаг, детали — в существующих
разделах. Не собирать все таблицы/метрики/формы в одну панель; не дублировать
вкладки, не уменьшать шрифт/отступы, чтобы всё вместить. Иерархия и свободное
пространство сохраняются на desktop и 393px; нет горизонтального overflow,
мелких безымянных иконок и действий, доступных только при hover.

Методические источники проверены 08.09: [Nielsen heuristics](https://www.nngroup.com/articles/ten-usability-heuristics/),
[Fitts](https://www.nngroup.com/videos/fittss-law-links-buttons/),
[ясное различие вариантов](https://www.nngroup.com/articles/explicit-differences/),
[ограничения правила трёх кликов](https://www.nngroup.com/articles/3-click-rule/),
[W3C target size](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html).
44px — наш ориентир удобства, не утверждение о минимуме WCAG: SC 2.5.8 задаёт
24×24 CSS px либо предусмотренные исключения. Присланные владельцем статьи
сохранены в product.md как происхождение его требований.
Pending/error-поведение реализовывать существующими form actions; актуальная
официальная основа проверена через Context7:
[useActionState](https://react.dev/reference/react/useActionState) и
[useFormStatus](https://react.dev/reference/react-dom/hooks/useFormStatus).

### Быстрый порядок и честная проверка

1. План зафиксирован docs-only PR #686, merged `61733a15`. После этого владелец
   явно поручил выполнить новый план: «and do the plan as well, this new plan».
   Исполнение ведётся небольшими проверенными частями; merged не означает deployed.
2. До реализации определить контракты событий/видимости и переноса открытых
   задач. UX-2 и UX-3 можно вести независимо; UI-правки идут в той же небольшой
   части. Миграционные номера и merge координирует один исполнитель.
3. UX-4 собирает события UX-2/3; UX-6 добавляет свои события в ту же проекцию.
   UX-8 можно вести параллельно после фиксации разрешённой student-проекции.
   Не держать готовые права/формы до готовности всей истории и замещения.
4. Для docs — classifier, whitespace, links, short `Release contracts` и
   независимое exact-head review. Для реализации — затронутые реальные
   RPC/Auth/browser пути, повторное чтение записи и роль-отказы; один desktop
   и mobile проход по изменённым сценариям с keyboard/состояниями/скриншотами.
   Не повторять full Supabase/build/browser suite на каждую правку; новые
   forward migrations получают соответствующую реальную DB-проверку.
5. Нет настоящего аккаунта Admissions/student, реального дела или разрешённого
   бизнес-действия — назвать точный пробел проверки. Не создавать synthetic
   users/cases/files или mock-success ради зелёного результата; Admin preview
   не доказывает права другого реального actor. Production данные, migration
   apply, deployment и отправки остаются отдельными gates. QR не запрашивать.

### Текущая передача исполнения

- База: `main` `61733a155d3926742144db21e53e6396da249864` (PR #686).
- Ветка исполнения: `izzhackt/curator-ux-execution`; проверяемый код
  `c35f8c1a` (последующие docs-only commits не означают новое runtime proof).
- Интеграционный worktree:
  `/Users/iskhak.tazhibaev/.codex/worktrees/run-plan-handover/evo_AI_CRM`.
  Исходный checkout с чужими изменениями не чистили и не переключали.
- Все части собраны на одной ветке, без второго приложения и без смены Auth.
  Ни одна migration129–133 не применена к production; новый UI не развёрнут.

| Часть | Подготовлено в коде | Отложенная проверка на настоящем деле |
| --- | --- | --- |
| UX-2 / 129 | Собственные срок/приоритет, причина, точная ссылка на задачу; черновик переживает stale и требует явной сверки | Изменение → readback/audit; чужая задача, исполнитель/видимость и stale отклоняются |
| UX-3 / 130 | Принять передачу/запросить уточнение; ответ связан с текущим назначением, Sales видит только свой краткий ответ | Настоящий куратор/Sales, повтор запроса, переназначение away/back |
| UX-4 / 132 | Одна разрешённая лента в профиле, ссылки, словарь, дедупликация и keyset-пагинация | Существующие и новые события всех разрешённых типов, границы ролей, отсутствие дублей |
| UX-6 / 133 | Нагрузка Admin, точный предпросмотр, атомарные start/return; новые задачи заместителя учтены | Реальный перенос/возврат, конкурентная новая задача, stale/ручное назначение, нет оставшихся задач ушедшего куратора |
| UX-8 / 131 | Новый v2: этап, отдельные действия студента/EVO, срок, куратор, точный документ; неизменённый v1 только для rollback | Вход настоящего студента, соответствие реальным записям, правильная ссылка и отсутствие внутренних данных; после принятия удалить v1 отдельной forward migration |

По решению владельца третья колонка не блокирует текущую выкладку и не считается
пройденной. Её выполняют после deployment, когда появятся согласованные реальные
дела и роли; технические release gates из этого решения не исключены.

UX-8 exit inventory для review и release:

- `supabase/migrations/131_platform_student_portal_next_steps.sql` — только новый
  v2; DDL старого v1 здесь запрещён;
- `src/lib/v3/portal-source.ts` — только вызов v2, без fallback;
- `tests/v3-student-portal-source.test.mjs` и
  `tests/v3-student-portal-ui.test.mjs` — source contracts v2/no-fallback/v1
  preservation;
- `supabase/tests/platform_student_portal_next_steps_boundary.sql` и его вызов из
  `scripts/test-postgres-authorization.sh` — реальные catalog/no-session проверки
  обеих форм без создания actors/cases;
- будущая migration удаления v1 не входит в текущую ветку и создаётся только
  после явной приёмки владельца; номер заранее не резервировать.

### Что уже проверено — и чего эти проверки не доказывают

- Node `22.23.1`: общий `npm run typecheck`, ESLint изменённых TS/TSX и
  `git diff --check` проходят. Новые Node contracts зарегистрированы по одному
  разу; реестр `--suite unit --validate-only` содержит 122 уникальных файла.
- 19 узких source-contract проверок проходят. Они проверяют код/SQL и словарь,
  но не заменяют действие реального пользователя. Не выдавать unit/source
  assertions за реальный RPC, браузер или работу провайдера.
- На OrbStack создана только отдельная schema-only база
  `evo_curator_ux_rehearsal_20260908` внутри `supabase_db_evo-platform-local`.
  Восстановлена локальная схема125 со штатными owners/ACL без данных; migrations
  126–133 применены по порядку от `postgres`. Пользователей и дел в ней 0.
  Рабочую локальную БД и production не меняли.
- Реальный PostgreSQL выполнил три catalog/no-session boundary файла:
  `platform_handoff_acknowledgement_boundary.sql`,
  `platform_case_activity_boundary.sql`, `platform_curator_coverage_boundary.sql`.
- Все три проходят, включая реальный RPC-отказ для PostgreSQL дат `infinity`
  и `10000-01-01`, не поддерживаемых UI. `plpgsql_check` новых PL/pgSQL путей
  не нашёл ошибок; остались только предупреждения о неиспользуемых переменных
  перенесённых старых task bodies. Новые coverage routines — без предупреждений.
- Code-review нашло и закрыло три дефекта: потерю task draft при
  stale (`655cb299`, regression `75bbd8ac`) и отсутствие подписи handoff-события,
  ломавшее историю (`1c6c3c95`), а также недопустимую дату возврата замещения
  (`c35f8c1a`). Исправления независимо одобрены; все пять частей получили
  code approval. Это не acceptance approval. Точные review targets — в ledger.
- Полный release suite, production migration apply и deployment не запускались.
  Ошибочно начатый одним исполнителем полный локальный migration harness был
  остановлен (exit130), его точный disposable container удалён; результата
  приёмки от этого запуска нет. В итоговой schema-only проверке не создавались
  тестовые дела, пользователи или подставные клиентские данные.

### Что не доказано и точное продолжение

После разблокировки Mac прочитан настоящий Admin UI: `/v3/profile` показывает
0 доступных дел и «В базе нет ни одного человека»; календарь сообщает, что нет
активного дела для задачи. `http://localhost:3000` — SSH-туннель к production,
**не** локальный экземпляр изменённого кода. Проверка этого экрана не доказывает
новый UI. Прямой service-key запрос к таблицам вернул 403; его не обходили.
Отсутствие реального дела больше не блокирует deployment по явному решению
владельца, но соответствующая приёмка остаётся непройденной.

1. Прочитать этот план и ledger, затем `git fetch origin`, проверить ветку,
   HEAD, `git status`, разницу с актуальным `origin/main` и GitHub PR state.
   Не повторять завершённый первый запуск и не терять локальные чужие изменения.
2. Продолжать из актуального checkpoint [Sales-плана](sales-report-run-plan.md):
   исходная копия, импортёр и bounded implementation уже подготовлены. Закончить
   exact-head review и нерешённые release/auth blockers; не повторять экспорт
   и не создавать synthetic business data.
3. Выполнить независимое exact-head review, выбранные репозиторием PR/CI gates и
   merge согласно launch-control. После последней правки заново зафиксировать
   точный current-main release SHA; старый `c35f8c1a` — только code checkpoint.
4. До любых production writes прочитать фактическую migration history и
   подтвердить backup/rollback. Поскольку ledger применяет 129 раньше 131,
   проверить одобренный Admin-only null-reason guard в canonical body 129/133;
   portal bridge сам по себе этот риск не закрывает.
   После применения reviewed schema проверить обе точные overview формы/grants и
   обновлённый PostgREST schema cache. Старое приложение остаётся на v1, затем
   выкладывается exact-image нового приложения, которое вызывает лишь v2. При
   rollback вернуть прежний image: он использует сохранённый v1; общий DB recovery
   остаётся обязательным release evidence. Не удалять v1 в этой волне.
5. Для замороженного exact SHA выполнить обязательный технический release path:
   требуемый full CI, schema/app cutover по утверждённой процедуре, downstream
   deploy, exact-image и health/readback, а также доказательства rollback.
   Реальные provider calls и synthetic production данные не использовать;
   изолированные технические CI fixtures разрешены отдельно.
6. После deployment владелец проверяет интерфейс. Когда появятся согласованные
   реальные дела и роли, отдельно выполнить отложенные Curator/Sales/Student
   happy paths и существенные отказы, durable readback/audit и один browser-проход
   desktop+393px. До этого checkbox не закрывать и acceptance не объявлять.
