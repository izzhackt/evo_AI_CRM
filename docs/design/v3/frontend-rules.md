# Что нельзя выводить на экран V3

Этот файл — не стиль и не вкусовщина. Здесь перечислено то, что уже было
сделано неправильно, поймано заказчиком и убрано. **Читайте до того, как
добавите на экран новое поле.**

Правила писались после аудита прототипа V3: 17 находок на четырёх частях,
29 мест. Все они — один и тот же класс ошибки, повторённый в разных местах.

---

## Правило 1. Ключ из базы не попадает на экран никогда

`handed_off`, `draft`, `pending`, `active`, `application.created` — это имена
в базе, а не слова, которыми говорят люди.

Перевод живёт **в одном месте**: `src/lib/v3/wording.ts`. Не переводите по
месту, в компоненте: второй словарь разойдётся с первым, и никто не заметит.

**Почему это не косметика.** У заявки в вуз в этом же продукте два разных
словаря статусов:

    канонический   draft / submitted / accepted / rejected / withdrawn
    платформенный  preparation / ready / submitted / under_review / offer /
                   rejected / enrolled / withdrawn / closed

Слово на экране зависит от того, из какой таблицы его прочитали. Человеку
показывали `draft`, хотя то же самое в другой части продукта называется
`preparation`. Пока на экране человеческое слово, такое расхождение видно;
пока ключ — нет.

**Неизвестное значение не показывается сырым.** `wording.ts` возвращает `null`,
и вызывающий не рисует ничего. Показать ключ «на всякий случай» — это вернуть
ровно то, что убирали.

## Правило 2. Значение, одинаковое во всех строках, ничего не сообщает

Пилюля `admissions` в каждой из двадцати задач не различает ни одной из них.
То же было с `whatsapp` на каждом диалоге и `open` на каждом.

**И это не про засеянные данные — это про схему.** Проверьте `CHECK`, прежде
чем рассуждать «в проде разойдётся»:

    evo_leads.owner_role          = 'sales'         всегда
    evo_student_cases.owner_role  = 'admissions'    всегда
    evo_admissions_tasks.assigned_role = 'admissions'  всегда
    evo_university_applications.owner_role = 'admissions'  всегда
    evo_visa_milestones.owner_role = 'admissions'   всегда
    evo_conversations.channel     = 'whatsapp'      всегда

Эти поля не могут принять другое значение: база не даст. Значит и на экране
они не различают ничего — никогда, а не «пока».

Если значение одно на весь список — назовите его **один раз сверху** и уберите
из строк. Если оно ограничено `CHECK` одним значением — его не должно быть на
экране вообще.

## Правило 3. Цвет обязан значить состояние записи

В мире V3 цвет живёт только в статус-пилюлях (`src/components/v3/Pill.tsx`).
Тон, назначенный вручную «чтобы было живее», — украшение, притворяющееся
данными. Если настоящего состояния нет, пилюля серая.

Отсюда же: **подкрашивать панель, колонку или строку нельзя.** Выделение — это
пилюля, ребро или почти чёрный акцент.

## Правило 4. Поле на экране — потому что на него смотрят, а не потому что оно есть в модели

`owner_role` в шапке досье — это про то, кто ведёт, а не про человека.
`target_intake` — часть подписи заявки, а не отдельный статус.

Прежде чем добавить поле, ответьте: **какое решение человек примет, увидев
его?** Нет ответа — поля на экране нет.

## Правило 5. Из нового мира не ведут ссылки в старый

Ссылки «Lead 360» и «Открыть профиль» вели на `/sales/<id>` — страницу V2.
Нажатие выкидывало из нового интерфейса в тот, от которого уходим.

Исключение ровно одно и оно оговорено заказчиком: карточка воронки продаж
открывает профиль лида — так было в задании.

## Правило 6. Одна страница на человека

Лид и студент — один человек на разных стадиях, а не два объекта на экране.
В базе это `evo_leads` и `evo_student_cases` (связаны через `lead_id`), но
интерфейс про человека, а не про таблицы.

Сделано в `src/components/v3/Profile.tsx`:

- состояние — **одна строка** («Студент», «Лид · на квалификации»), а не
  россыпь пилюль; считается в `personState()`;
- пока он лид, блоков «Заявки» и «Виза» **нет вовсе**. Пустая рамка с надписью
  «пока ничего» сообщала бы о недоделке там, где всё идёт по плану;
- продажная часть у лида раскрыта («Продажа» — это его текущая работа), у
  студента свёрнута в «Как он к нам пришёл». Свёрткой служит нативный
  `<details>`: открывается с клавиатуры без единой строки JS.

## Правило 7. Не дублируйте часть

`/v3/funnel` был той же фигурой, что и блок на главной: один компонент, один
источник, те же данные, разница только в размере. Часть удалена.

«Задачи» дублировали левую колонку календаря — там та же очередь дел. Удалены.

## Правило 8. Не показывайте одно и то же дважды

Отдельной вкладки «Файлы» в профиле нет намеренно: она перечисляла бы те же
паспорт.pdf и аттестат.pdf, что уже стоят в строках чеклиста документов. Файл
живёт при своём пункте; в «прочие файлы» попадает только то, что ни к одному
пункту не относится.

По той же причине **«Обзор» не повторяет содержимое вкладок**. Он отвечает на
один вопрос — что с человеком сейчас и куда идти дальше, — и ведёт в нужную
вкладку. Сводка, дублирующая все вкладки, обесценивает сами вкладки.

## Правило 9. Вкладка — это ссылка, а не виджет

Адрес несёт `?tab=`, поэтому вкладку можно переслать, вернуться назад кнопкой
браузера и открыть без JavaScript. Роли `tablist`/`tab` были бы неправдой: это
переходы по страницам, и читалка должна назвать их ссылками. Текущая помечена
`aria-current="page"`.

## Правило 10. Образец отделён от настоящего в типах, а не в комментарии

В профиле их два: `PersonProfile` — то, что читается из базы, и `ProfileDraft`
— то, чего в модели ещё нет (анкета, план платежей, типы документов,
ответственный). Образец лежит на странице, а не в компоненте.

Если поле переехало из образца в базу, это видно по тому, что оно поменяло
тип. Комментарий «здесь пока заглушка» такой гарантии не даёт — он устаревает
молча.

На экране такие поля подчёркнуты пунктиром. Не пилюлей и не цветом: цвет
означает состояние записи, и тратить его на «это пока картинка» значило бы
сломать правило ради временной пометки.

---

# Что именно убрано и когда

Дата: 02.09.2026. Аудит по прототипу V3, решения приняты заказчиком.

| где | было | стало | почему |
|---|---|---|---|
| Досье, шапка | `active` | — | статус кейса одинаков у всех, человека не описывает |
| Досье, шапка | `handed_off` | строка состояния | ключ из базы |
| Досье, шапка | `admissions` | — | роль — не свойство человека |
| Досье, шапка | `referral` | — | источник не нужен в шапке досье |
| Досье, шапка | ссылка «Lead 360» | — | вела в старый V2 |
| Досье, заявка | `draft` | «не подана» | ключ из базы |
| Досье, заявка | пилюля `Fall 2026` | «набор Fall 2026» в подписи | это не статус |
| Досье, виза | 6× `pending` | одна строка в шапке блока | шесть одинаковых пилюль — один факт |
| Досье, история | `application.created` | «Заявка заведена» | ключ из базы |
| Досье, история | `draft → open` | — | сырые состояния |
| Входящие, список | `whatsapp` | — | канал одинаков у всех диалогов |
| Входящие, список | `sales` | — | ключ из базы |
| Входящие, шапка | `new` | — | ключ из базы |
| Входящие, шапка | `open` | — | одинаков у всех диалогов |
| Входящие, шапка | ссылка «Открыть профиль» | — | вела в старый V2 |
| Воронка, карточка | `referral · 02.09` | `02.09` | на карточке просили только имя |
| — | часть «Задачи» | удалена | дублировала календарь |
| — | часть «Воронка поступления» | удалена | дублировала блок на главной |

**Из базы не удалено ничего.** Все поля остаются в модели и приходят в
компоненты; убрано только то, что рисуется. В типах пропсов такие поля помечены
комментарием «есть в модели, намеренно не рисуется».

# Что осталось машинным намеренно

Экран «Настройки»: `sales.read`, `/clients`. Он про политику в коде, и
человеческая формулировка завела бы второй словарь, который разойдётся с
настоящим. Адрес страницы и имя права — это и есть тот факт, который экран
показывает.
