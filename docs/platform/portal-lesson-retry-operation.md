# Урок: повтор именно отказавшей операции

Дата: 2026-09-21. Узкий блок B24 после merged #1015.
База `436865af5d5453b7136856d67ed1d9562260b27c`.
Статус на 2026-09-22: **реализация, независимое source review и узкий local actual приняты**.
[Реальная проверка и её границы](../qa/portal-lesson-retry-actual-2026-09-22.md).
Финальная интеграция main отдельно проходит exact-head review и CI перед merge.
Precode-контракт записан до кода в `0eea93dcf64074e06a8a75f1a753e9f5cb2805dd`.

## Дефект и ожидаемый результат

В `LessonRunner.tsx` последовательность «конфликт → загрузить сохранённое →
сбой сети → повторить» выбирает другую команду. `reloadDraft` возвращает ошибку
`unavailable`, но обработчик повтора выбирает start/save/complete по состоянию
экрана; варианта reload в нём нет. При неотвеченном упражнении он может снова
отправить оставшийся ответ или ничего не сделать, если ответ пуст. При другом
состоянии вызывает завершение. Серверные проверки ревизии и идемпотентности
сохранены; успешная лишняя запись или потеря данных этим source-разбором не
установлены.

После изменения кнопка повторяет ту операцию, которой принадлежит ошибка.
Сбой загрузки сохранённого состояния повторяет только существующий read-action.
Вызовы start/save/complete продолжают использовать прежние request IDs,
ожидаемые ревизии и сохранённые снимки ответов.

## Принятый контракт — Impeccable Operate / harden

- Явная локальная идентичность операции `start | answer | complete | reload`.
  Returned failure и throw записывают её вместе с существующим кодом ошибки.
  При новой операции и успехе прежняя принадлежность ошибки сбрасывается.
  Не сохранять замыкание со старым payload, не создавать общий runner framework.
- Ручной retry выбирает только сохранённую операцию; busyRef предотвращает
  повторную команду во время ожидания. Ни автоматического повтора, ни новой
  команды вместо reload нет. Ref допустим только для обработчиков, не для
  вычисления JSX; отображение ошибки остаётся существующим React state.
- Сохраняются «Атлас», Golos, компоновка, кнопки/подписи RU/KY, локальный alert,
  pending/focus, черновик до подтверждённого чтения, exit guard, содержание
  упражнений и серверная оценка. Actions, RPC, Auth/RLS, SQL и другие runners
  не меняются.
- Изменение ограничено `LessonRunner.tsx`, адресной проверкой его маршрутизации
  повторов и этой документацией. Допустима прямая unit-проверка настоящего
  обработчика с простыми счётчиками выбранных операций; это не RPC/service mock
  и не доказательство Student/UI-прохождения. Новый тестовый стек не добавляется.

## Проверки и actual

Поведенческая regression должна отличать reload от save/complete при разных
состояниях экрана, возвращённом отказе и throw, а также проверять блокировку
дубля. Неизменность pendingSave/startRequestId/completeRequestId проверяется
в diff; профильные чистые learning/i18n проверки, scoped lint, TypeScript и
`git diff --check` дополняют независимое source review. Полный набор продукта,
build и локальная QA-среда автоматически не запускаются.

Приёмка требует настоящего draft/revision conflict существующего Student.
Для получения конфликта обычными действиями в двух вкладках конечная схема и
точные допустимые записи согласованы до запуска. Fixtures, прямую смену ревизий
или подмену ответов не использовали. Последующий допущенный сценарий описан
в квитанции от 22 сентября ниже.

На source checkpoint локальная среда была недоступна: координатор сообщил
об отсутствии OrbStack socket. Это был инфраструктурный блокер actual; Docker
в этом блоке не запускали и не перезапускали. Тогда PR оставался draft с QA pending.
Последующая согласованная проверка на 001–238 и закрытие среды приняты 22 сентября;
её квитанция выше не заменяет сохранённую историю первоначального ограничения.

## Основание и визуальная преемственность

Независимый precode review `6f458939e77b244ac86844f670854fce7fae762fab59c5c55ad700e6e137c185`:
`/private/tmp/evo-b24-lesson-retry-precode-review-20260921.md`.
Принятый brief `c75ca02bc8e3300535467150c4e2270449b63193b4bd3beed1370242092d084b`;
source/visual inventory `095cdcc601e1e05cd7a286eca276e77d68a3a02360929a7a6631bf0df115f9a1`.

Просмотрены существующие lesson-answer320/1440 из [принятого local234](../qa/portal-learning-feedback-2026-09-21.md).
Восемь файлов LessonRunner/exercises/actions/types/portal CSS/global CSS/layout/
Shell побайтно совпадают с проверенным `f77a457b`; английские строки не менялись.
Это визуальная база и доказательство прежнего положительного ответа, не actual
конфликта/ошибки загрузки. Общий пункт24, native и production не завершены.

Официальные источники проверены 2026-09-21:
[React useRef](https://react.dev/reference/react/useRef) — значение сохраняется
между renders; ref для обработчика не вызывает render и не читается в JSX.
[Responding to Events](https://react.dev/learn/responding-to-events) — действия
выполняются обработчиками событий. Соответствие повтора прежнему намерению —
требование нашего UX-контракта, а не новое правило сервера.

## Исторический source checkpoint — 2026-09-21

Исходники зафиксированы в `0f5fca638f195967d0601427e228b264ef43bd90`.
`failedOperation` хранит только имя операции. Returned failure и throw каждой
из четырёх команд записывают это имя до показа ошибки. Начало допущенной команды
сбрасывает прежнее имя; retry с busyRef ничего не запускает. Payload или старое
замыкание не сохраняются. Все остальные байты компонента совпадают с precode
после исключения этих точечных изменений; параметры actions, request IDs,
pendingSave, success/focus и оформление сохранены.

Проверки выполнены Node `22.23.1`, затем повторно не запускались:

- Две regression-проверки на исходном компоненте дали ожидаемый FAIL: вместо
  reload выбирались answer и complete. Raw CFW span `01a0c55676b076d398f1eceb332e9957`.
- `node --test --experimental-strip-types tests/portal-lesson-retry.test.mjs tests/portal-learning.test.mjs tests/portal-i18n.test.mjs` — **20/20 PASS**:
  пять новых проверок выбора операции, семь learning и восемь i18n.
  Raw CFW span `01a0c5577dcf7060b78cf341d704bdad`.
- `node node_modules/eslint/bin/eslint.js src/components/portal/english/LessonRunner.tsx tests/portal-lesson-retry.test.mjs`
  — PASS, exit0; span `01a0c55784177df0919b60eb8eb2949f`.
- `node node_modules/typescript/bin/tsc --noEmit --incremental false` — PASS,
  exit0; span `01a0c557a0d17f71b2c29dc157545bdf`.
- `git diff --check` — PASS. Дополнительная побайтная сверка неизменённой части
  компонента — PASS; span `01a0c55a765f71529c3099b7e3f57626`.

Новый тест исполняет настоящий локальный onClick и reportFailure; счётчики
проверяют только выбор команды. Обе reload-failure ветки сверены по AST;
сетевой throw или RPC response в браузере этим не исполняются. React render,
Student/Auth/DB путь и сохранение после retry остаются непроверенными.
Новых зависимостей, build, сервиса или QA-записей этот блок не создавал.
Существующее предупреждение Node о MODULE_TYPELESS_PACKAGE_JSON не меняли.
Source review, protected CI и согласованный реальный сценарий ещё нужны;
PR остаётся draft, весь пункт24 и production не закрыты.

## Принятый local actual — 2026-09-22

На source c59e93cf обычный Student подтвердил conflict → offline reload →
manual retry: повтор загрузил revision 3 той же read-командой. Ровно один
сохранённый ответ принят; strict final290/33 и own local logout204/закрытие
ресурсов прошли. Независимое actual/closure review `752f71b0` принято ROOT.
Первый closure OSError STOP сохранён; причина неизвестна, последующая отдельная
разрешённая проверка закрытия завершилась успешно без повтора UI или записи.

Интеграция main eec9c548 не меняет LessonRunner или его regression test.
Source review c59 `f7a403f3` и CI 35642597272 относятся именно к прежнему head;
[actual-квитанция](../qa/portal-lesson-retry-actual-2026-09-22.md) разделяет
переиспользуемое доказательство и обязательные final-head review/CI.
Остальные runners, native, весь пункт24 и production этим блоком не закрыты.
