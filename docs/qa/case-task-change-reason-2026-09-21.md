# Обязательная причина изменения задачи: фактическая проверка

Срез 21.09.2026. Source `9f63d87a4c340e445f091e5578578baa935236b3`,
миграция222 SHA256 `f2e0c8bd31b5ad0bb19d1b21e9569b98be1ec5dc6e83facb1521a1f5075e3909`.
После интеграции main `43bd20c802ef2e9789463548ef1a1e3c3cbcf09d` в
`12a1fc00` SQL и десять затронутых зависимостей UI/команды побайтно прежние.
Проверка выполнена только в существующем owned-local проекте
`evo-local-0fd3559d0240c989`. Production не изменялся.

## Изменение и сохранённые границы

Убрано временное исключение Admin из проверки причины при фактическом изменении
приоритета, даты или точного срока case-task. Пустая причина больше не разрешена
ни одной уполномоченной роли. Остальной canonical command не переписан:
права, coverage/locks, replay, optimistic version, остальные изменения задачи,
сигнатуры и публичные wrappers сохранены. Личные рабочие задачи не менялись.

Миграция проверяет полный исходный `prosrc` и единственный заменяемый фрагмент;
после CREATE OR REPLACE проверяет точную замену и все остальные атрибуты pg_proc.
До изменения MD5 тела `adf134e0faddd76e58e820100701caec`, после
`b5069fbe19b46e75605a71a1c79eddd2`. Независимые source и apply-packet review пройдены.

Один локальный apply подтвердил схему001–222, неизменность прежнего ledger,
ACL/owner/других атрибутов целевой функции, всех остальных функций,281 business
таблицы и Auth counts. Docker endpoint, socket, running container ID и project/
workdir labels проверены; CLI и наблюдатели привязаны к одному локальному endpoint.
Receipt `local222-receipt.json` SHA256:
`46f82535c8ae328753958e3d8f5895f3d0c3c7e149725e8ca0b103b149fbd536`.

## Обычные Auth-сессии и существующая задача

Использованы существующие Admin, Admissions, Sales и Student; новые аккаунты,
права, дела, задачи и fixtures не создавались. Свежие authority/target readers
подтвердили допустимый активный handed-off case и доступ Admin/Admissions.
Student подтверждён через current_actor_authority; отсутствие staff snapshot
не подменяло доказательство его роли. Sales/Student не получили target/command.

Все payload/request IDs зафиксированы до передачи. Выполнено17 command attempts:

- Шесть отказов22023: NULL для нового приоритета, даты и точного срока; пустая,
  состоящая из пробелов и превышающая1000 символов причина. Ещё два42501 —
  Sales и Student. Состояние и аудит не менялись.
- Четыре успешных Admin RPC: временный приоритет, срок на весь день, срок на
  12:00 Бишкека, затем восстановление исходных бизнес-полей с явной причиной.
- Exact replay вернул тот же результат без нового эффекта. Изменённые payload
  и причина с прежним request ID получили22023; два устаревших запроса получили
  PT409, включая запрос без причины. Порядок replay/version/reason сохранён.

Перед каждой попыткой проверено отсутствие постороннего изменения. Восстановление
выполнено с ожидаемой версией, без удаления истории: исходные бизнес-поля задачи
восстановлены, версия выросла на4, сохранены4 audit-события. Новых task transition
событий или уведомлений не было. Scoped-наблюдение после каждой попытки и полные
снимки до/после подтвердили неизменность остальных данных.

## Реальная форма

Обычный Local Admin открыл существующую задачу через
`/v3/tasks?type=case&kind=case&status=all&case=…&task=…`.
Использован runtime `3972eab1ab7cc681d57d07ea965a031bbd7fef90`; десять файлов формы,
server action, deadline helpers и authority побайтно совпали с source222.
Интерфейс этим PR не менялся; он уже требовал причину.

После выбора будущей даты браузер заблокировал пустую причину и сфокусировал
поле. Три пробела дали «Укажите причину изменения.». Для обеих попыток CDP
не зарегистрировал сетевых запросов; успешное UI-сохранение не заявляется.
Четыре успешные записи выше выполнены настоящими Auth RPC. Две приватные
картинки и `root222-ui-receipt.json` сохраняют фактический результат.
Собственная вкладка закрыта, devserver33230 остановлен.

## Результат, первоначальные отказы и ограничения

Финальный handover snapshot содержит281 таблицу: изменены только case_tasks и
четыре новые записи audit_events;279 остальных таблиц, Auth counts, схема,
функции и ledger неизменны. Независимое review сохранённых receipts одобрено.
`root222-release-receipt.json` SHA256:
`0c029bc47e627dc6c3b6a3c44f5ab16b0e14d0205ed8e5e7ec42a781243b4bec`.

Первый readiness остановился ДО Auth на слишком строгом regex наблюдателя:
он не принимал две существующие таблицы с суффиксом_v1. Исходный STOP сохранён;
исправлен только безопасный identifier guard, новый hash проверен независимо.
Успешный harness SHA256
`eb146c5ab08817e3458351829744d33fb54a23a49d23b8b622cb734e3212535f`.
Журнал и binding не заменялись. Первый offline handover comparison также
остановился: старый B218 aggregate other_rows.audit_events включал эти четыре
новые записи. Ожидание исправлено по тому же сохранённому снимку и доказательству
четырёх audit-событий, без повторного SQL/Auth/business исполнения.

Не доказаны replay старого исторического NULL-reason command и новый cross-tenant
сценарий. Проверка одной существующей задачи не является общим E2E. Preview,
изменение приоритета через UI и production deployment не заявляются.
Issue687 остаётся открытым до его managed/owner acceptance; уже удалённый
portal-v1 этот PR повторно не удаляет. Последующие полные проверки не требуются
для неизменного source: повторять только при новом изменении или конкретном риске.
