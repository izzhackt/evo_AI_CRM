# CRM-05 — завершённая передача в сопровождение

Статус: локальная RPC и реальная доска проверены. Source review двумя независимыми
агентами одобрено; окончательный merge зависит от review финальной дельты и CI.
Production и managed Supabase не менялись.

## Проверяемый исходник и границы

- Runtime: `55581820dd939cde83f8dd6038f1c76df714e9d3`, контракт до кода — `3f74cc45`.
- Миграция 212: SHA-256 `619e25b1e3f41a389e25837e1105d39a04c7f6c562a173baae5cab6cdb58882c`.
- План: CRM-05, пункты 6 и 10. Само наличие студенческого дела не означает
  завершённую передачу. Терминальная колонка сохраняется.
- Доказательство: completed-запись 088/174 либо совпадающие create receipt,
  pipeline provenance `pending_case` и связанное дело ветки 208. Финансовый
  архив, текущий куратор и текущее состояние дела не отменяют факта передачи.
- Новый read-only ответ содержит только ID разрешённого открытого лида, признак
  и время передачи. Case IDs, финансовые поля и provenance не выдаются.
- Старые RPC, workflow-команды, их DTO и текущая каноническая воронка 210 не менялись.

## Реальное локальное выполнение

Разрешённый существующий локальный проект `evo-local-0fd3559d0240c989`, Supabase
на loopback. После завершения B211 применена ровно следующая миграция через
Supabase CLI `migration up --local`: проверены исходный SHA, непрерывная
история 001–211 и точный hash 211; результат — 001–212. Не использовались reset,
managed API, создание пользователей, изменение ролей или новые бизнес-записи.

Проверка `/tmp/evo-crm-05-existing-inputs-check.mjs --existing-local-read-only`
выполнилась 20.09.2026 в 19:20:40 UTC на runtime выше. Это отдельная локальная
техническая проверка, не customer/provider acceptance и не финальный E2E.

| Проверка обычной аутентификации и RPC | Фактический результат |
| --- | --- |
| Admin | 7 разрешённых лидов, 4 завершённые передачи; группировка new 3 / handed_off 4 |
| Sales | 7 разрешённых лидов, 4 завершённые передачи; группировка new 3 / handed_off 4 |
| Admissions без `sales.register.read` | 4 разрешённых лида, все 4 передачи видны |
| Admissions: один свой и один действительно недоступный существующий лид | отказ всему batch, `42501`, данных нет |
| Student / anonymous | соответственно HTTP 403 / 401; данных нет |
| Пустой список доступного staff | полный пустой ответ, не ошибка |
| Повтор одного существующего ID в запросе | `22023`, данных нет |
| Каноническая воронка 210 | состав и counts совпадают с прежней scoped lead queue |

У каждого возвращённого лида сверены точный набор полей, факт и timestamp
передачи с существующими durable-записями БД. У Admin/Sales 210 по-прежнему
считает new 6 / qualified 1; это канонические этапы открытых лидов, а не
операционная группировка доски. Равенство этих двух группировок не заявляется.

До/после: Auth users 6, leads 7, cases 7, sales 4 и handoffs 4; всё без изменений.
Входов `pending_case` 208 в локальной БД нет. Закрытые лиды, архивирование,
другой tenant, граница 4000 и конкурентное изменение authority не запускались;
соответствующие ветки проверены только по исходнику. Preview и недоступный или
повреждённый ответ прекращают чтение; их браузерное выполнение пока не заявлено.

Приватные mode-0600 receipts остаются вне Git в существующем локальном QA parent:
`local212-receipt.json`, `a212-existing-inputs-receipt.json`. Секреты, Auth payloads
и идентификаторы бизнес-записей в этот документ не включены.

## Проверки исходника

Node 22.23.1, `npm ci --ignore-scripts`, typegen + TypeScript, scoped ESLint для
двух адаптеров и pglast parse SQL прошли. Из трёх выбранных существующих V3
static guards два прошли (handoff surface и keyboard links); inventory остаётся
красным из-за ранее существовавшего пропуска `knowledge-library-source.ts`.
Новый `sales-handoff-source.ts` внесён в обе группы inventory; этот несвязанный
KB-пропуск не исправлялся. Полный suite не запускался.

## Фактическая доска после интеграции каталога

UI проверен на `91e30f510a22ac498f64f21785944682937a3bd7`: включён main
`1e03c6bede4e368fb895b0b81863289670d9632d` (#944). Runtime212 побайтно совпадает
с уже проверенным55581820, а reader211/SQL211 — с main. Повторные typegen и
TypeScript после merge прошли; неизменный RPC-прогон не повторялся.

В существующей обычной Sales-сессии Chrome, собственный dev33218:

- Без фильтров: Новый3 / Переданы4. «Новый» оставляет3 карточки, «Переданы» —4,
  «Квалифицирован» —0: единственный canonical qualified уже передан.
- Поиск `QA B209` даёт2 существующие карточки и сохраняет2 при сочетании
  «Без ответственного» + «Без следующего действия». Фильтр конкретного Sales
  даёт3 переданные карточки; четвёртая принадлежит другому сотруднику.
- У новых3 карточек доступны disclosures действий, у переданных4 их нет.
  Enter действительно открыл форму решения нового лида; сохранение не вызывалось.
- Desktop viewport2016, mobile390 и320: document scrollWidth не превышает
  viewport. На mobile группы расположены вертикально, все7 карточек сохраняются;
  колонка Переданы визуально показывает4. Основные input/select/buttons —44px.
  Tab показывает2px outline у native select; ссылка карточки также имеет видимый
  keyboard focus. Эмуляция после проверки снята.
- Скриншоты и DOM-derived receipt: ignored `.next/crm-05-proof/`:
  `sales-desktop.png`, `sales-mobile-390.png`, `sales-mobile-320.png`,
  `sales-mobile-320-handoff.png`, `ui-receipt.json`. Один ограниченный проход,
  без изменений CSS, создания записей и подмены ответов.

Отдельно обнаружена прежняя неточность формы фильтра: после «Сбросить всё» URL и
доска уже не фильтруются, но uncontrolled select сотрудника сохраняет выбранный
ранее option до reload. `pipeline/page.tsx` с defaultValue не изменён этим PR;
наблюдение записано в очередь последующего блока фильтров. Оно не меняет факт
передачи или серверное ограничение чтения. Общая готовность всех фильтров/UX
не заявляется. Preview/error/truncation и >20 terminal cards в браузере не
индуцировались; ограничения runtime-проверки выше сохраняются.
