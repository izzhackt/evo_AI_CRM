# Компактная сводка продаж — локальная проверка 21 сентября 2026

Узкий срез пункта7: сводка найденных записей и отдельный план отдела.
Actual candidate `f59adb7034c22fcf76d21f18699674deb0297663`; baseline
`0fd6952a2796178e1d540f454742ded2f42bcee2`. После проверки интеграция main
`ca7c98ec2efc0f8f74eea4e6dd9541ff09c48699` сохранила SalesRegisterView byte-for-byte
(SHA256 `dbf6a4058c96be779ed4ed7e30fe89f0af4cdf847702c254050c85999be8418f`).
Это повторное использование доказательства неизменённого экрана, не новый UI-прогон.

## Что проверено

Обычный вход существующих локальных Sales и Admin; сентябрь2026, тот же запрос
без изменения записей или создания fixtures. Последовательно baseline/candidate
при1440/390/320px:12 состояний в одном визуальном проходе,24 сохранённых снимка
области и viewport. Impeccable Operate/distill/adapt: сохранены EVO, Golos,
семантические заголовки валют и пары label/value. DOM/CSS для снимков не изменялись.

Для обеих ролей совпали две доступные записи, их ссылки, суммы KGS1/0 и
USD1000/0, а также предупреждение об одной неуточнённой оплате. Неуточнённая
оплата в записи осталась «Не уточнено», не заменена нулём. План виден только
Admin и остаётся «Не задан». Положительный cash-блок отсутствовал в обеих версиях:
его числовое содержимое и finance-permission discrepancy этим срезом не проверены.

На390/320px прежняя таблица сводки требовала400px и горизонтального скролла.
Новая сводка укладывается в ширину, без clipping/overflow; валюты не смешиваются.
Высота Sales1440/390/320 уменьшилась с261.25/390.89/433.14 до192.25/340.5/382.75px.
Admin1440/390 немного выше прежнего из-за отдельного пояснения плана, Admin320
уменьшился с512.53 до472.75px. Универсальное уменьшение высоты не заявляется.
Сохранённые AX-представления связывают значения с названиями; screen-reader
acceptance отдельно не проводилась. Значок Next.js на снимках относится к dev-среде.

## Сохранность и завершение

Локальная схема001–230: все287 бизнес-таблиц, каталог, финансовые эффекты,
Storage objects/buckets совпали с входящим A15f состоянием. Обычный logout обеих
собственных сессий восстановил точные224 входящие строки сессий; историческая
сессия ROOT из предыдущего блока сохранена. Собственные браузеры28232/29070
закрыты, порт33242 освобождён; сохранённые токены собственных сессий удалены.
Auth user rows могут отражать обычный вход; полного равенства всех Auth-полей
до/после login эта проверка не утверждает.

Private evidence: `/private/tmp/evo-sales-summary-after230-preparation-20260921`.
Release `218dba4e4b0949cc1e0155a79a668f29fceb5499cda0f217d52b54d42a474966`
передал окно ROOT231. Независимый actual review
`1e89c86afa8f36513d44eace22c26c2481b8a7350b1ad73bb482e9f0af93a2fc` — APPROVED.
Исходные данные и токены не публикуются в репозитории.

Это локальная приёмка изменения сводки. Направления из полного разрешённого
набора, расположение импорта, отдельная несогласованность прав финансового
плана, весь пункт7 и доставка в production остаются отдельной работой.
