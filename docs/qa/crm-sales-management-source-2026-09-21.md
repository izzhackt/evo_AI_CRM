# План отдела и права управления: проверка исходников

21 сентября2026, пункт7 / CRM-02f. Base maince0ef573 после #1001; precode1370c1c.
Миграция235 зарезервирована ROOT после234. Этот документ не подтверждает
применениеSQL, обычный Auth, реальный интерфейс или production.

Новый read-only RPC возвращает из одного STABLE snapshot существующие
organization-scope права target.manage/import и выбранный план отдела. Успешный
отказ отделён от разрешённого отсутствия. На новом endpoint дополнительно
закреплён нынешний report-entry gate; старые v1/v2 не изменены. Nullable month
означает запрос только прав, без подстановки сегодняшнего месяца.

Строгий шестипольный DTO проверяет org/month/schema, booleans, точную BIGINT
version и только department target. Нельзя принять target при denied/no-month.
View больше не использует финансовое право или withheld workspace.targets
для вывода «Не задан». Касса сохраняет свой отдельный существующий reader.
При unknown формы target/import остаются на прежнем месте со стабильной
идентичностью, сохранённым вводом/File и блокировкой отправки. Подтверждённый
отказ скрывает форму. Сами SQL-команды и server actions не менялись.

Impeccable Operate/craft-floor использован с текущим EVO и сохранёнными233
incumbent captures. Они показывают предыдущую версию, не приёмку новогоUI.
Перенос импорта в управление остаётся следующим отдельным7b.

Проверки:8 целевых pure DTO tests — PASS, Node22.23.1; scoped ESLint четырёх
затронутыхTS/TSX-файлов — PASS; обычный npm run typecheck — PASS; git diff --check —
PASS. Существующий pglast7.7 разобрал все6 SQL statements и PL/pgSQL-body; SQL не
исполнялся. Тесты декодера не заменяют Auth/tenant или UI proof. Новые зависимости,
fixtures, аккаунты, записи планов или импорт не создавались.

Далее после release A234 и B1003: привязанный локальный apply235, ordinary
существующие staff/Student и read-only интерфейс с полной сверкой до/после.
Отдельно проверить доступ, выбранный/пустой месяц, сохранение черновика после
refresh, год/архив и невозможность отправки при неизвестном доступе в реально
доступных состояниях. Отсутствующие комбинации grants/данных не фабриковать.
