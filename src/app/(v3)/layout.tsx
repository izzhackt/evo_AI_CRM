import type { ReactNode } from "react";

import "./v3.css";

/**
 * Оболочка V3.
 *
 * Направление взято из присланного референса: тёплая подложка, на ней —
 * «плавающая» белая поверхность с крупным скруглением и мягкой тенью, много
 * воздуха, лёгкая хрома.
 *
 * Токены переопределяются здесь, а не в компонентах: части уже написаны на
 * именах `--surface`, `--text`, `--accent`, поэтому смена мира — это смена
 * значений в одном месте, а не правка каждого экрана. Основное приложение
 * (V2) не затронуто: переопределение живёт внутри `.v3-world`.
 */
export default function V3Layout({ children }: { children: ReactNode }) {
  return <div className="v3-world">{children}</div>;
}
